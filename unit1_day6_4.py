'''Your task is to:
1. Take two integers a and b as input from the user 2. Compute the expression (a + b)³ > = a + b², and print the result on the
console.
2. Compute the expression (a+b)^3 >= a^4+b^2, and print the result on the console.
3. Compute the expression (a + b) // (a % b), and print the result on the console.'''
a=int(input())
b=int(input())
print((a+b)**3>=a**4+b**2)
print((a + b) // (a % b))